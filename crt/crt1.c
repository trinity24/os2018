#include <stdlib.h>

void _start(void) {
  
	//TODO: Main should also accept arguments and environment settings
	/*
	int argc=1;
	char *argv[10];
	char *envp[10];
	*/
 	main(0,NULL, NULL); //and 
	exit(1);
}
